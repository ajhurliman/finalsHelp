# Bounce people to the new servers

server {
    listen      80 default_server;

    location /secondaryFlow {
        return 302 https://haldev.hq.halw.com$request_uri;
    }

    location /seabourn {
        return 302 https://sbndev.hq.halw.com$request_uri;
    }
}
